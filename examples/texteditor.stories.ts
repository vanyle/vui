import type { Meta, StoryObj } from "@storybook/web-components-vite";
import "./texteditor";

const meta: Meta = {
    component: "vui-texteditor",
};

export default meta;
type Story = StoryObj;

export const Primary: Story = {
    args: {},
};
